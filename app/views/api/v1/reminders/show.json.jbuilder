# json.extract! @restaurant, :id, :name, :address
# json.comments @restaurant.comments do |comment| ### je cree une clé .comments, je donne en attribut @restaurant.comments et             j’affiche chaque commentaire
#     json.extract! comment, :id, :content
# end
